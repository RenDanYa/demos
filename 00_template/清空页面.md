<%*
// 使用 Obsidian API 清空当前文件内容
const currentFile = app.workspace.getActiveFile();
if (currentFile) {
    await app.vault.modify(currentFile, "");
    new Notice("✅ 页面内容已清空");
} else {
    new Notice("❌ 无法获取当前文件");
}
%>