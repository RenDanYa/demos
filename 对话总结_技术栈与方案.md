# 数据分析技术栈与方案总结

## 一、Excel数据处理（已完成）

### 项目背景
4张数据源表按物料编号关联，输出成果表8列：
- 商品描述、Division → XLOOKUP查找
- 净金额、数量 → SUMIFS汇总（取绝对值）
- 当日库存、库存尺码 → 从库存表清洗后取
- 出样 → 拼接收据号|数量

### 已生成的程序
| 文件 | 功能 |
|---|---|
| 生成成果表.py | 弹窗选Excel → 生成成果表+库存明细 |
| 导入数据库.py | 生成成果表 + 导入SQLite数据库（5张表） |
| 业务看板.py | Streamlit 5页看板（直连数据库+Plotly图表） |
| app_streamlit.py | Streamlit简易版（上传Excel处理） |
| 验证数据库.py | 检查数据库数据完整性 |

### 数据库结构（业务数据库.db）
| 表名 | 行数 | 关键字段 |
|---|---|---|
| 商品主数据 | 130,757 | 物料编号、商品描述、Division |
| 销售流水 | 623 | 商品编号、净金额、数量 |
| 出样记录 | 1,380 | 物料编号、收据号、数量 |
| 库存明细 | 130,737 | 物料编号、尺寸、库存量 |
| 成果表 | 587 | 8列成果数据 |
每张表都加了「导入日期」列，支持去重和趋势分析

---

## 二、技术栈选型

### 编程语言：Python
- AI辅助生成代码，但需要能读懂、能调试、能运行
- 不用学写代码，但必须学读代码和跑代码
- pandas核心：read_excel / groupby / merge / to_sql

### 数据库：SQLite起步
| 维度 | SQLite | MySQL | PostgreSQL |
|---|---|---|---|
| 安装 | Python自带，零安装 | 要装服务 | 要装服务 |
| 适合 | 单人、十万级 | 多人、百万级 | 数据分析最强 |
| 统计函数 | 基础 | 有标准差，无中位数 | 全（中位数、百分位） |
- SQLite数据就是一个.db文件，Navicat直接打开文件即可连接

### BI工具：Power BI Desktop
- 免费、和Excel同源、国内JD出现率最高
- 学习时间3-5天（只学拖拽+建关系，公式让AI写）
- 拖拽操作AI替代不了，但DAX公式可以
- 帆软FineBI：国产、企业级、收费贵（按用户数+功能模块收费）
- Tableau：效果最好但收费贵

### 图表库：Plotly（配合Streamlit）
| 方案 | 特点 |
|---|---|
| Streamlit内置 | 一行代码，5种基本图表，样式固定 |
| Plotly | 40+种图表，交互最强，推荐 |
| Pyecharts | ECharts的Python版，效果最好 |
| ECharts | JS图表库，最灵活但要写前端 |

---

## 三、整体架构设计

### 完整技术栈
```
数据层：Excel → SQLite（零安装）
ETL：  Python pandas 定时清洗入库
后端：  Python Flask 或 Streamlit
图表：  Plotly 或 Streamlit内置
定时：  Windows计划任务
日志：  Python logging
```

### 自动化流程
```
每天定时：python 导入数据库.py → 数据入库
   ↓
随时打开：Streamlit看板 → 自动查最新数据
   ↓
按需刷新：点刷新按钮 → 图表更新
   ↓
AI分析：在TraeWork里问问题 → 帮你写SQL
```

### 什么时候升级
| 阶段 | 用什么 | 触发条件 |
|---|---|---|
| 现在 | Excel + Python + SQLite + Streamlit | 起步阶段 |
| 第一步 | 加Windows计划任务 | 需要每天自动跑 |
| 第二步 | 换MySQL | 多人同时访问 |
| 第三步 | 上DB-GPT | 需要网页对话查数据库 |
| 第四步 | 考虑Django | 需要多用户权限系统 |

---

## 四、Streamlit看板设计

### 5个页面
| 页面 | 内容 |
|---|---|
| 📊 销售概览 | 4指标卡 + 饼图 + 柱状图 + 直方图 + 明细折叠 |
| 📈 趋势分析 | 折线图 + 柱状图 + 环比变化（多天数据） |
| 📦 库存看板 | 尺码分布 + 库存频次 + 库存明细 |
| 📋 明细数据 | 5张表切换 + 模糊搜索 + 下载 |
| 📥 数据导入 | 拖拽上传 + 进度条 + 入库 + 下载 |

### 布局工具
| 工具 | 作用 |
|---|---|
| st.columns | 横向分列（支持比例如[3,1]） |
| st.tabs | 同区域切换视图 |
| st.expander | 默认折叠收起 |
| st.sidebar | 侧边栏放筛选和导航 |
| st.container(border=True) | 带边框分组 |
| @st.cache_data(ttl=300) | 5分钟缓存避免重复查库 |

---

## 五、关键概念区分

### 网站开发 vs Agent开发
- 网站开发：你写死逻辑，用户点按钮执行固定流程（Flask/Django/Streamlit）
- Agent开发：AI自己决定每一步做什么（TraeWork就是Agent）

### 框架轻重对比
```
Streamlit（最轻）→ 数据看板，不会前端也能用
Flask（轻量）→ 简单Web接口
Django（重量）→ 完整网站系统（自带用户登录、后台管理、权限）
```

### AI编程工具对比
| 工具 | 形态 | 特点 |
|---|---|---|
| TraeWork | 全能AI工作台 | 工作+编程，数据分析+文档+PPT |
| Cursor | AI代码编辑器 | VS Code魔改版，实时补全 |
| Claude Code | 终端AI代理 | 读大型代码库、排障 |
| Codex | ChatGPT云端代理 | 并行多任务、自动提PR |
- 用户量：Codex约1500万，TRAE约600万（国内第一）

### 数据敏感度判断
| 数据类型 | 能否用TraeWork |
|---|---|
| 公开数据 | 可以 |
| 公司内部业务数据 | 看公司政策 |
| 客户个人信息/财务数据 | 不建议直接传给AI |
| 解决方案 | AI只写代码，数据本地跑（安全的做法） |

### TraeWork无网使用
- AI能力需要联网（模型在云端）
- 方案：有网时用TraeWork生成代码 → 无网时本地运行代码
- 内网如能访问trae.cn，大概率能用

---

## 六、数据库连接工具

| 工具 | 免费 | 特点 |
|---|---|---|
| DB Browser for SQLite | ✅ | 专为SQLite，最推荐 |
| DBeaver | ✅ | 万能，多种数据库都能连 |
| Navicat | 收费 | 功能全体验好 |
| VS Code插件 | ✅ | 不装新软件 |
| Python代码 | ✅ | sqlite3模块直接查 |
- SQLite连接Navicat：选SQLite → 选.db文件 → 不填密码 → 连接

---

## 七、AI SQL

### 两种路线
| 路线 | 代表 | 能直接执行 |
|---|---|---|
| 通用AI | TraeWork/ChatGPT | 不能，需手动复制SQL执行 |
| 专用AI SQL | DB-GPT（国产开源）/ Vanna.ai | 能，直连数据库自动查自动出图 |

### 国产工具
- DB-GPT：国内最火的开源AI SQL工具，可本地部署
- 通用AI国产：TraeWork、豆包、通义千问、文心一言、Kimi

---

## 八、运行方式

### 本机运行Streamlit看板
```bash
pip install streamlit plotly pandas openpyxl
python -m streamlit run 业务看板.py
```
浏览器自动打开 http://localhost:8502

### 本机运行导入数据库
```bash
python 导入数据库.py
```
弹窗选Excel → 自动生成数据库+Excel文件
